import { MessageBox, Message } from "element-ui";

export const callMessage = Message;
export const callMessageError = Message.error;
export const callMessageSuccess = Message.success;
export const callMessageInfo = Message.info;
export const callAlert = MessageBox.alert;
export const callConfirm = MessageBox.confirm;
